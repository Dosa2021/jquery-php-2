version: "3.8"

services:
  web:
    build: .
    container_name: php-apache
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    networks:
      - app-network
    # depends_on:
    #   - mysql

networks:
  app-network:
    driver: bridge
